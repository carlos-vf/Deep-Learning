# Example usage: yolo train data=dataset.yaml
# parent
# ├── src
# └── dataset

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: Dataset # dataset root dir
train: Training_and_validation # train images (relative to 'path')
val: Training_and_validation # val images (relative to 'path') 
test: Test # (optional)

# Classes
names:
  1: fish1
  2: fish2
